using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OpenObstacle : AbstractObstacle
{
    
    // Start is called before the first frame update
    void Start()
    {
        Dangerous = false; 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

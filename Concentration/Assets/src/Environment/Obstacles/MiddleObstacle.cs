using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MiddleObstacle : AbstractObstacle
{
    // Start is called before the first frame update
    void Start()
    {
        Dangerous = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

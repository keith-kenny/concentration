using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EdgeObstacle : AbstractObstacle
{
    
    // Start is called before the first frame update
    void Start()
    {
        // Globals
        Dangerous = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
